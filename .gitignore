__pycache__/
*.py[cod]
.idea/
.venv/
venv/
save_data.json

# pygbag build output - regenerated by the GitHub Actions Pages workflow
# on every push; web-cache/ in particular holds a machine-local copy of
# the wasm Python runtime, not something to version.
build/
